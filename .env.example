# Public build-time values only. Do not add credentials or private tokens.
# Omit these locally to use the root URL and http://localhost:3000.
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_DISABLE_REACT_DEVTOOLS=
